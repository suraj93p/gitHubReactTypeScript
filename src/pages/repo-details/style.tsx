import styled from 'styled-components';
import { deviceWidths } from '../../config/config';

export const RepoListPageWrapper = styled.div`
    padding: 16px;
`;

export const ReadmeWrapper = styled.div`
    padding: 16px;
`;